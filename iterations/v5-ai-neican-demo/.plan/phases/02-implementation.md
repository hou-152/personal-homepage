# Phase 02 · Implementation

## 目标

把 AI 内参信息流 Demo 加入首页。

## 范围

- 新增 `src/data/ai-neican-demo.ts`。
- 更新 `src/App.tsx`，新增 Demo 组件和导航入口。
- 更新 `src/data/projects.ts`，让 AI 内参项目卡片跳到 Demo。
- 更新 `src/styles/global.css`，补充响应式交互样式。
- 更新 `src/data/build-log.ts`。

## 已完成

- 新增 `src/data/ai-neican-demo.ts`。
- 新增首页 `AiNeicanDemo` 交互区块。
- 更新项目卡片 Demo 跳转。
- 补充响应式样式和 Build Log。

## 状态

完成。
